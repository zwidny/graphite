============
Installation
============

At the command line::

    $ easy_install project_directory

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv project_directory
    $ pip install project_directory
